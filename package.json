{
  "name": "national-voting-system",
  "version": "2.0.0",
  "private": true,
  "description": "MERN + Hardhat blockchain voting system with an admin blockchain explorer",
  "workspaces": [
    "blockchain",
    "backend",
    "frontend"
  ],
  "scripts": {
    "chain": "npm run node -w blockchain",
    "compile": "npm run compile -w blockchain",
    "deploy": "npm run deploy:local -w blockchain",
    "server": "npm run dev -w backend",
    "client": "npm run dev -w frontend",
    "seed": "npm run seed -w backend",
    "reset-votes": "npm run reset-votes -w backend",
    "dev": "concurrently -k -n chain,api,web -c yellow,cyan,green \"npm run chain\" \"wait-on -t 60000 tcp:127.0.0.1:8545 && npm run deploy && npm run server\" \"npm run client\"",
    "build": "npm run build -w frontend",
    "start": "npm start -w backend",
    "test": "npm test -w blockchain && npm test -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "wait-on": "^8.0.5"
  },
  "license": "MIT"
}
